import React from 'react';
import Layout from '../components/Layout';

// Placeholder — replaced in Phase 8
const MyBookings = () => (
  <Layout>
    <div className="container py-5 text-center">
      <h2 className="fw-bold" style={{ color: '#12315C' }}>My Bookings</h2>
      <p className="text-muted">Booking list with status badges + meeting link coming in Phase 8</p>
    </div>
  </Layout>
);

export default MyBookings;
