import React from 'react';
import Layout from '../components/Layout';

// Placeholder — replaced in Phase 7
const MentorDetail = () => (
  <Layout>
    <div className="container py-5 text-center">
      <h2 className="fw-bold" style={{ color: '#12315C' }}>Mentor Profile</h2>
      <p className="text-muted">Mentor detail + open slots + Book button coming in Phase 7</p>
    </div>
  </Layout>
);

export default MentorDetail;
