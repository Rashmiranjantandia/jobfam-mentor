import React from 'react';
import Layout from '../components/Layout';

// Placeholder — replaced in Phase 6
const Profile = () => (
  <Layout>
    <div className="container py-5 text-center">
      <h2 className="fw-bold" style={{ color: '#12315C' }}>My Profile</h2>
      <p className="text-muted">Profile editing + tag management coming in Phase 6</p>
    </div>
  </Layout>
);

export default Profile;
